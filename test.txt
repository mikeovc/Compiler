@@
 int i,max,sum;
@@
 sum := 0;
 i := 1;
 read(max);

 while (i < max) do
	sum := sum + i;
	i := i + 1;

write(sum+max);

/* 

test if statemetns, bools

 if (high <= step)
   { write(low);}
 endif

 if (high = true)
   { 
     read(low);
     abc := 3;
   }
 else
   return 5.23;
 endif

*/